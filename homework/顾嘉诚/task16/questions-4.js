//4.遍历数组，把数组里的打印数组每一项的平方 （难度**）
var arr = [3,4,5];
for(var i=0;i<arr.length;i++){
	console.log(arr[i]*arr[i]);
}
/*
	var arr = [3,4,5];
	for(value in arr){
		console.log(arr[value]*arr[value]);
	}
	//对数组使用for in,表示的是在数组中的位置
*/
